계속해서 더 많은 파일 예시를 보여드리겠습니다!

---

## 4. 📋 더 많은 Slash Commands

### .claude/commands/generate-docs.md

```markdown
---
name: generate-docs
description: MIL-STD-498 표준 문서 자동 생성
---

# Generate Documentation Command

## 문서 생성 프로세스

### 1. 코드에서 요구사항 추출

다음 명령 실행:
\`\`\`
코드베이스를 분석해서 다음을 추출해줘:

1. 모든 [REQ-*] 주석에서 요구사항 목록
2. 각 요구사항의 구현 위치
3. 테스트 커버리지 상태

결과를 requirements-traceability-matrix.md로 저장
\`\`\`

### 2. API 문서 생성

\`\`\`bash
# XML 주석에서 API 문서 생성
docfx metadata
docfx build
\`\`\`

### 3. 아키텍처 다이어그램 업데이트

\`\`\`
프로젝트 구조를 분석해서 다음 다이어그램을 Mermaid로 생성해줘:

1. 시스템 아키텍처 다이어그램
2. 데이터 흐름 다이어그램
3. 시퀀스 다이어그램 (주요 유스케이스)

각 다이어그램을 docs/diagrams/ 에 저장
\`\`\`

### 4. 변경 이력 문서화

\`\`\`
Git 로그를 분석해서 CHANGELOG.md 업데이트:
- 마지막 릴리스 이후 변경사항
- 버전별 주요 기능
- 버그 수정 목록
\`\`\`

## 생성될 문서 목록

- `requirements-traceability-matrix.md`: 요구사항 추적표
- `api-reference.html`: API 레퍼런스
- `architecture-overview.md`: 아키텍처 개요
- `CHANGELOG.md`: 변경 이력
- `deployment-guide.md`: 배포 가이드
```

---

### .claude/commands/run-integration-test.md

```markdown
---
name: run-integration-test
description: 통합 테스트 실행 및 결과 분석
---

# Run Integration Test Command

## 테스트 실행 절차

### 1. 환경 준비

\`\`\`bash
# 테스트 데이터베이스 초기화
.\scripts\setup-test-db.ps1

# 테스트 서버 구성 확인
.\scripts\verify-test-environment.ps1
\`\`\`

### 2. 통합 테스트 실행

\`\`\`bash
# 전체 통합 테스트
dotnet test --filter "Category=Integration" --logger "trx;LogFileName=integration-results.trx"

# 결과 분석
.\scripts\analyze-test-results.ps1 integration-results.trx
\`\`\`

### 3. 실패한 테스트 분석

\`\`\`
테스트 결과를 분석해서 다음을 리포트해줘:

1. 실패한 테스트 목록 및 원인
2. 성능 저하가 있는 테스트 (기준 대비 50% 이상 느림)
3. 불안정한 테스트 (Flaky tests)
4. 커버리지 부족 영역

각 문제에 대한 수정 방안도 제시해줘.
\`\`\`

### 4. 성능 벤치마크

\`\`\`bash
# BenchmarkDotNet 실행
dotnet run --project Tests/Benchmarks --configuration Release

# 결과 비교
.\scripts\compare-benchmarks.ps1 baseline.json current.json
\`\`\`

## 통과 기준

- [ ] 모든 통합 테스트 통과
- [ ] 성능 저하 없음 (기준 대비 ±10% 이내)
- [ ] 메모리 누수 없음
- [ ] 데드락 또는 타임아웃 없음
```

---

### .claude/commands/refactor-legacy.md

```markdown
---
name: refactor-legacy
description: 레거시 코드를 안전하게 리팩토링
---

# Refactor Legacy Code Command

## 리팩토링 안전 절차

### 1단계: 기존 동작 보호

\`\`\`
현재 파일의 모든 public 메서드에 대해 
Characterization Test를 생성해줘.

목표: 현재 동작을 있는 그대로 캡처
- 정상 입력 케이스
- 경계값 케이스
- 예외 케이스
\`\`\`

### 2단계: 코드 분석

\`\`\`
다음 관점에서 코드를 분석해줘:

1. 단일 책임 원칙 위반
2. 중복 코드
3. 긴 메서드 (100줄 이상)
4. 복잡도가 높은 메서드 (McCabe > 15)
5. 강한 결합

각 문제에 우선순위를 매겨줘.
\`\`\`

### 3단계: 점진적 리팩토링 계획

\`\`\`
리팩토링 계획을 세워줘:

각 단계마다:
1. 변경 내용
2. 위험도 평가 (Low/Medium/High)
3. 테스트 전략
4. 롤백 계획

원칙:
- 한 번에 하나씩만 변경
- 각 단계 후 테스트 실행
- 기능 변경 없이 구조만 개선
\`\`\`

### 4단계: 실행

\`\`\`
계획의 첫 단계를 실행하고,
테스트가 모두 통과하는지 확인해줘.

그 다음 단계로 진행하기 전에 내 승인을 받아.
\`\`\`

## 리팩토링 체크리스트

- [ ] Characterization 테스트 작성 완료
- [ ] 모든 테스트 통과
- [ ] 코드 리뷰 승인
- [ ] 성능 저하 없음 확인
- [ ] 문서 업데이트
```

---

## 5. 🪝 Hooks 상세 예시

### .claude/hooks/pre-commit.ps1

```powershell
#!/usr/bin/env pwsh
<#
.SYNOPSIS
    커밋 전 품질 검사
.DESCRIPTION
    보안, 코드 품질, 테스트를 자동으로 검사합니다.
#>

param(
    [switch]$SkipTests,
    [switch]$SkipSecurity
)

$ErrorActionPreference = "Stop"
$exitCode = 0

function Write-Header {
    param([string]$Title)
    Write-Host "`n" -NoNewline
    Write-Host ("=" * 70) -ForegroundColor Cyan
    Write-Host " $Title" -ForegroundColor Cyan
    Write-Host ("=" * 70) -ForegroundColor Cyan
}

function Write-Check {
    param([string]$Message)
    Write-Host "  🔍 $Message..." -ForegroundColor White
}

function Write-Success {
    param([string]$Message)
    Write-Host "  ✅ $Message" -ForegroundColor Green
}

function Write-Warning {
    param([string]$Message)
    Write-Host "  ⚠️  $Message" -ForegroundColor Yellow
}

function Write-Error {
    param([string]$Message)
    Write-Host "  ❌ $Message" -ForegroundColor Red
}

# ============================================================================
# 1. 변경 파일 분석
# ============================================================================
Write-Header "변경 파일 분석"

$changedFiles = git diff --cached --name-only --diff-filter=ACM
$csFiles = $changedFiles | Where-Object { $_ -match '\.cs$' }
$testFiles = $changedFiles | Where-Object { $_ -match 'Tests?\.cs$' }

Write-Host "  변경된 C# 파일: $($csFiles.Count)개"
Write-Host "  변경된 테스트 파일: $($testFiles.Count)개"

if ($csFiles.Count -eq 0) {
    Write-Warning "C# 파일 변경 없음. 일부 검사 생략."
}

# ============================================================================
# 2. 보안 스캔
# ============================================================================
if (-not $SkipSecurity) {
    Write-Header "보안 스캔"
  
    # 2.1 민감정보 스캔
    Write-Check "하드코딩된 자격증명 검사"
    $sensitivePatterns = @(
        'password\s*=\s*["''][^"'']+["'']',
        'apikey\s*=\s*["''][^"'']+["'']',
        'secret\s*=\s*["''][^"'']+["'']',
        'connectionstring\s*=\s*["''].*password.*["'']'
    )
  
    $securityIssues = @()
    foreach ($file in $csFiles) {
        $content = Get-Content $file -Raw
        foreach ($pattern in $sensitivePatterns) {
            if ($content -match $pattern) {
                $securityIssues += "${file}: 민감정보 감지"
            }
        }
    }
  
    if ($securityIssues.Count -gt 0) {
        Write-Error "보안 문제 발견:"
        $securityIssues | ForEach-Object { Write-Host "    $_" -ForegroundColor Red }
        $exitCode = 1
    } else {
        Write-Success "민감정보 없음"
    }
  
    # 2.2 취약한 암호화 검사
    Write-Check "취약한 암호화 알고리즘 검사"
    $weakCryptoPatterns = @(
        'DESCryptoServiceProvider',
        'TripleDESCryptoServiceProvider',
        'MD5CryptoServiceProvider',
        'SHA1CryptoServiceProvider'
    )
  
    $cryptoIssues = @()
    foreach ($file in $csFiles) {
        $content = Get-Content $file -Raw
        foreach ($pattern in $weakCryptoPatterns) {
            if ($content -match $pattern) {
                $cryptoIssues += "${file}: $pattern 사용"
            }
        }
    }
  
    if ($cryptoIssues.Count -gt 0) {
        Write-Error "취약한 암호화 발견:"
        $cryptoIssues | ForEach-Object { Write-Host "    $_" -ForegroundColor Red }
        $exitCode = 1
    } else {
        Write-Success "안전한 암호화 사용"
    }
  
    # 2.3 SQL Injection 검사
    Write-Check "SQL Injection 취약점 검사"
    $sqlInjectionPatterns = @(
        'string\.Format.*SELECT',
        '\$".*SELECT.*\{',
        '\$".*DELETE.*\{',
        '\$".*UPDATE.*\{',
        '\+ .*SELECT',
        '\+ .*DELETE',
        '\+ .*UPDATE'
    )
  
    $sqlIssues = @()
    foreach ($file in $csFiles) {
        $content = Get-Content $file -Raw
        foreach ($pattern in $sqlInjectionPatterns) {
            if ($content -match $pattern) {
                $sqlIssues += "${file}: SQL Injection 가능성"
            }
        }
    }
  
    if ($sqlIssues.Count -gt 0) {
        Write-Error "SQL Injection 취약점 발견:"
        $sqlIssues | ForEach-Object { Write-Host "    $_" -ForegroundColor Red }
        $exitCode = 1
    } else {
        Write-Success "SQL Injection 취약점 없음"
    }
}

# ============================================================================
# 3. 코드 품질 검사
# ============================================================================
Write-Header "코드 품질 검사"

# 3.1 빌드
Write-Check "프로젝트 빌드"
$buildOutput = dotnet build --no-restore 2>&1
if ($LASTEXITCODE -ne 0) {
    Write-Error "빌드 실패"
    Write-Host $buildOutput -ForegroundColor Red
    $exitCode = 1
} else {
    Write-Success "빌드 성공"
}

# 3.2 코드 포맷 체크
Write-Check "코드 포맷 검사"
$formatOutput = dotnet format --verify-no-changes --verbosity quiet 2>&1
if ($LASTEXITCODE -ne 0) {
    Write-Error "코드 포맷 불일치"
    Write-Host "  다음 명령으로 자동 포맷: dotnet format" -ForegroundColor Yellow
    $exitCode = 1
} else {
    Write-Success "코드 포맷 일치"
}

# 3.3 정적 분석 (Roslyn Analyzers)
Write-Check "정적 분석 실행"
$analyzerOutput = dotnet build /p:EnforceCodeStyleInBuild=true /p:TreatWarningsAsErrors=true 2>&1
if ($LASTEXITCODE -ne 0) {
    Write-Error "정적 분석 경고 발견"
    # 경고 내용 파싱 및 출력
    $warnings = $analyzerOutput | Select-String "warning CS" | Select-Object -First 10
    $warnings | ForEach-Object { Write-Host "    $_" -ForegroundColor Yellow }
    if ($warnings.Count -eq 10) {
        Write-Host "    ... (더 많은 경고 있음)" -ForegroundColor Yellow
    }
    $exitCode = 1
} else {
    Write-Success "정적 분석 통과"
}

# 3.4 복잡도 검사
Write-Check "코드 복잡도 검사"
foreach ($file in $csFiles) {
    $content = Get-Content $file
    $lines = $content.Count
  
    # 파일당 최대 라인 수 체크
    if ($lines -gt 500) {
        Write-Warning "${file}: ${lines}줄 (권장: 500줄 이하)"
    }
  
    # TODO/FIXME 개수 체크
    $todos = ($content | Select-String "TODO|FIXME").Count
    if ($todos -gt 5) {
        Write-Warning "${file}: TODO/FIXME ${todos}개 (권장: 5개 이하)"
    }
}

# ============================================================================
# 4. 테스트
# ============================================================================
if (-not $SkipTests) {
    Write-Header "테스트 실행"
  
    # 4.1 단위 테스트
    Write-Check "단위 테스트 실행"
    $testOutput = dotnet test --filter "Category=Unit" --no-build --logger "console;verbosity=minimal" 2>&1
    if ($LASTEXITCODE -ne 0) {
        Write-Error "단위 테스트 실패"
        Write-Host $testOutput -ForegroundColor Red
        $exitCode = 1
    } else {
        Write-Success "단위 테스트 통과"
    }
  
    # 4.2 변경된 코드에 대한 테스트 존재 확인
    Write-Check "테스트 커버리지 확인"
    foreach ($file in $csFiles | Where-Object { $_ -notmatch 'Tests?' }) {
        $className = [System.IO.Path]::GetFileNameWithoutExtension($file)
        $testFile = Get-ChildItem -Path . -Recurse -Filter "${className}Tests.cs" -ErrorAction SilentlyContinue
      
        if (-not $testFile) {
            Write-Warning "${file}: 테스트 파일 없음 (${className}Tests.cs)"
        }
    }
  
    # 4.3 커버리지 체크 (변경된 파일만)
    if ($csFiles.Count -gt 0) {
        Write-Check "코드 커버리지 측정"
        $coverageOutput = dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=json /p:CoverletOutput=coverage.json --no-build 2>&1
      
        if (Test-Path coverage.json) {
            $coverage = Get-Content coverage.json | ConvertFrom-Json
            $lineRate = [math]::Round($coverage.Summary.LineRate * 100, 1)
          
            if ($lineRate -lt 85) {
                Write-Warning "커버리지 부족: ${lineRate}% (목표: 85%)"
            } else {
                Write-Success "커버리지 충족: ${lineRate}%"
            }
          
            Remove-Item coverage.json -ErrorAction SilentlyContinue
        }
    }
}

# ============================================================================
# 5. 문서 검사
# ============================================================================
Write-Header "문서 검사"

# 5.1 XML 주석 체크
Write-Check "XML 주석 검사"
$missingDocs = @()
foreach ($file in $csFiles) {
    $content = Get-Content $file -Raw
  
    # public class/interface 체크
    $publicTypes = [regex]::Matches($content, 'public\s+(class|interface|enum)\s+(\w+)')
    foreach ($match in $publicTypes) {
        $typeName = $match.Groups[2].Value
        # 바로 위에 /// <summary> 가 있는지 체크
        $typePattern = "///\s*<summary>.*?</summary>.*?public\s+(?:class|interface|enum)\s+$typeName"
        if ($content -notmatch $typePattern) {
            $missingDocs += "${file}: ${typeName} (타입 주석 없음)"
        }
    }
  
    # public method 체크
    $publicMethods = [regex]::Matches($content, 'public\s+(?:async\s+)?(?:virtual\s+)?(\w+)\s+(\w+)\s*\(')
    foreach ($match in $publicMethods) {
        $methodName = $match.Groups[2].Value
        if ($methodName -notin @('Equals', 'GetHashCode', 'ToString')) {
            $methodPattern = "///\s*<summary>.*?</summary>.*?public.*?$methodName\s*\("
            if ($content -notmatch $methodPattern) {
                $missingDocs += "${file}: ${methodName}() (메서드 주석 없음)"
            }
        }
    }
}

if ($missingDocs.Count -gt 0) {
    Write-Warning "XML 주석 누락:"
    $missingDocs | Select-Object -First 5 | ForEach-Object { 
        Write-Host "    $_" -ForegroundColor Yellow 
    }
    if ($missingDocs.Count -gt 5) {
        Write-Host "    ... (${missingDocs.Count}개 중 5개만 표시)" -ForegroundColor Yellow
    }
} else {
    Write-Success "XML 주석 완비"
}

# 5.2 CHANGELOG 업데이트 확인
if (Test-Path CHANGELOG.md) {
    Write-Check "CHANGELOG 업데이트 확인"
    $changelogModified = git diff --cached --name-only | Where-Object { $_ -eq "CHANGELOG.md" }
  
    if ($csFiles.Count -gt 0 -and -not $changelogModified) {
        Write-Warning "코드 변경이 있지만 CHANGELOG.md가 업데이트되지 않음"
    } else {
        Write-Success "CHANGELOG 확인"
    }
}

# ============================================================================
# 6. Git 커밋 메시지 검증 (준비)
# ============================================================================
Write-Header "커밋 메시지 준비"

Write-Host @"
  
  커밋 메시지 형식:
  
  [타입] 간단한 설명 (50자 이내)
  
  상세 설명 (선택사항, 72자로 줄바꿈)
  
  타입:
    - feat: 새로운 기능
    - fix: 버그 수정
    - docs: 문서 변경
    - style: 코드 포맷
    - refactor: 리팩토링
    - test: 테스트 추가/수정
    - chore: 빌드/도구 변경
  
  예: [feat] 표적 추적 기능 추가
  
"@ -ForegroundColor Cyan

# ============================================================================
# 최종 결과
# ============================================================================
Write-Header "검사 완료"

if ($exitCode -eq 0) {
    Write-Host "`n  🎉 모든 검사 통과! 커밋을 진행하세요.`n" -ForegroundColor Green
} else {
    Write-Host "`n  ⛔ 검사 실패! 문제를 수정한 후 다시 시도하세요.`n" -ForegroundColor Red
    Write-Host "  팁: 일부 검사를 건너뛰려면 다음 플래그를 사용하세요:" -ForegroundColor Yellow
    Write-Host "    -SkipTests: 테스트 건너뛰기" -ForegroundColor Yellow
    Write-Host "    -SkipSecurity: 보안 스캔 건너뛰기" -ForegroundColor Yellow
}

exit $exitCode
```

---

### .claude/hooks/post-edit.ps1

```powershell
#!/usr/bin/env pwsh
<#
.SYNOPSIS
    파일 편집 후 자동 처리
.DESCRIPTION
    코드 포맷, 감사 로그, 백업 등을 수행합니다.
#>

param(
    [string[]]$EditedFiles
)

$ErrorActionPreference = "Continue"

Write-Host "`n🔧 Post-Edit Hook 실행 중...`n" -ForegroundColor Cyan

# ============================================================================
# 1. 자동 포맷
# ============================================================================
Write-Host "1. 코드 자동 포맷..." -ForegroundColor White

$csFiles = $EditedFiles | Where-Object { $_ -match '\.cs$' }

if ($csFiles.Count -gt 0) {
    foreach ($file in $csFiles) {
        Write-Host "   포맷: $file" -ForegroundColor Gray
    }
  
    # 변경된 파일만 포맷
    dotnet format --include $($csFiles -join ',') --verbosity quiet
  
    if ($LASTEXITCODE -eq 0) {
        Write-Host "   ✅ 포맷 완료`n" -ForegroundColor Green
    } else {
        Write-Host "   ⚠️  포맷 중 일부 문제 발생`n" -ForegroundColor Yellow
    }
}

# ============================================================================
# 2. 감사 로그 기록
# ============================================================================
Write-Host "2. 감사 로그 기록..." -ForegroundColor White

$logDir = ".claude/logs"
if (-not (Test-Path $logDir)) {
    New-Item -ItemType Directory -Path $logDir | Out-Null
}

$logFile = Join-Path $logDir "edit-history.log"
$timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
$user = $env:USERNAME
$hostname = $env:COMPUTERNAME

foreach ($file in $EditedFiles) {
    $logEntry = "[$timestamp] $user@$hostname edited: $file"
    Add-Content -Path $logFile -Value $logEntry
}

Write-Host "   ✅ 로그 기록 완료: $logFile`n" -ForegroundColor Green

# ============================================================================
# 3. 자동 백업 (중요 파일만)
# ============================================================================
Write-Host "3. 중요 파일 백업..." -ForegroundColor White

$criticalFiles = $EditedFiles | Where-Object { 
    $_ -match '(Service|Repository|Controller)\.cs$' 
}

if ($criticalFiles.Count -gt 0) {
    $backupDir = ".claude/backups/$(Get-Date -Format 'yyyyMMdd')"
    if (-not (Test-Path $backupDir)) {
        New-Item -ItemType Directory -Path $backupDir -Force | Out-Null
    }
  
    foreach ($file in $criticalFiles) {
        $backupName = "$([System.IO.Path]::GetFileNameWithoutExtension($file))_$(Get-Date -Format 'HHmmss')$([System.IO.Path]::GetExtension($file))"
        $backupPath = Join-Path $backupDir $backupName
        Copy-Item $file $backupPath
        Write-Host "   백업: $file -> $backupName" -ForegroundColor Gray
    }
  
    Write-Host "   ✅ 백업 완료`n" -ForegroundColor Green
}

# ============================================================================
# 4. TODO 추출
# ============================================================================
Write-Host "4. TODO 항목 추출..." -ForegroundColor White

$allTodos = @()
foreach ($file in $EditedFiles | Where-Object { $_ -match '\.(cs|md)$' }) {
    $content = Get-Content $file
    $lineNumber = 0
  
    foreach ($line in $content) {
        $lineNumber++
        if ($line -match '(TODO|FIXME|HACK|NOTE):\s*(.*)') {
            $allTodos += [PSCustomObject]@{
                File = $file
                Line = $lineNumber
                Type = $matches[1]
                Text = $matches[2].Trim()
            }
        }
    }
}

if ($allTodos.Count -gt 0) {
    $todoFile = ".claude/TODO.md"
    $todoContent = "# TODO List`n`n"
    $todoContent += "마지막 업데이트: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')`n`n"
  
    $grouped = $allTodos | Group-Object Type
    foreach ($group in $grouped) {
        $todoContent += "## $($group.Name)`n`n"
        foreach ($item in $group.Group) {
            $todoContent += "- [ ] **$($item.File):$($item.Line)** - $($item.Text)`n"
        }
        $todoContent += "`n"
    }
  
    Set-Content -Path $todoFile -Value $todoContent
    Write-Host "   ✅ TODO 목록 업데이트: $todoFile`n" -ForegroundColor Green
}

# ============================================================================
# 5. 의존성 분석 (선택적)
# ============================================================================
if ($csFiles.Count -gt 0) {
    Write-Host "5. 의존성 분석..." -ForegroundColor White
  
    $dependencies = @{}
    foreach ($file in $csFiles) {
        $content = Get-Content $file -Raw
        $usings = [regex]::Matches($content, 'using\s+([\w.]+);')
      
        $deps = $usings | ForEach-Object { $_.Groups[1].Value } | 
                Where-Object { $_ -notmatch '^System' } |
                Select-Object -Unique
      
        if ($deps.Count -gt 0) {
            $dependencies[$file] = $deps
        }
    }
  
    if ($dependencies.Count -gt 0) {
        $depsFile = ".claude/dependencies.json"
        $dependencies | ConvertTo-Json | Set-Content $depsFile
        Write-Host "   ✅ 의존성 정보 저장: $depsFile`n" -ForegroundColor Green
    }
}

# ============================================================================
# 6. 메트릭 수집
# ============================================================================
Write-Host "6. 코드 메트릭 수집..." -ForegroundColor White

$metrics = @()
foreach ($file in $csFiles) {
    $content = Get-Content $file
    $lines = $content.Count
    $codeLines = ($content | Where-Object { $_ -match '\S' -and $_ -notmatch '^\s*//' }).Count
    $commentLines = ($content | Where-Object { $_ -match '^\s*//' }).Count
  
    $metrics += [PSCustomObject]@{
        File = $file
        TotalLines = $lines
        CodeLines = $codeLines
        CommentLines = $commentLines
        CommentRatio = if ($codeLines -gt 0) { 
            [math]::Round($commentLines / $codeLines * 100, 1) 
        } else { 0 }
    }
}

if ($metrics.Count -gt 0) {
    Write-Host "`n   코드 통계:" -ForegroundColor Cyan
    $metrics | Format-Table -Property File, CodeLines, CommentLines, CommentRatio -AutoSize |
        Out-String | ForEach-Object { Write-Host $_ -ForegroundColor Gray }
}

# ============================================================================
# 완료
# ============================================================================
Write-Host "✅ Post-Edit Hook 완료!`n" -ForegroundColor Green
```

---

## 6. 📚 문서(docs) 상세 예시

### docs/standards/coding-convention.md

```markdown
# 방산 시스템 코딩 컨벤션

## 문서 정보
- 버전: 2.1.0
- 최종 수정: 2024-02-11
- 작성자: 기술위원회
- 보안 등급: 대외비

## 1. 파일 구조

### 1.1 파일 헤더

모든 소스 파일은 다음 헤더로 시작해야 합니다:

```csharp
/*
 * 파일명: TargetTrackingService.cs
 * 프로젝트: TacticalCommandSystem
 * 모듈: 표적 추적 시스템
 * 작성자: 홍길동 (hong@defense.mil)
 * 작성일: 2024-02-11
 * 보안등급: 대외비
 * 
 * 설명:
 *   실시간 표적 추적 및 위치 업데이트 서비스
 * 
 * 요구사항:
 *   REQ-TCS-001: 실시간 표적 추적
 *   REQ-TCS-002: 다중 센서 통합
 * 
 * 의존성:
 *   - ITargetRepository
 *   - ISecurityService
 *   - IAuditLogger
 * 
 * 변경이력:
 *   2024-02-11 v1.0.0 - 최초 작성 (홍길동)
 *   2024-02-15 v1.1.0 - 다중 센서 지원 추가 (김철수)
 */
```

### 1.2 네임스페이스 구조

```csharp
namespace TacticalCommand.Core.Domain.Entities
//      [제품명]    [계층] [역할]   [상세]
{
    // 코드...
}
```

계층별 네임스페이스:

* `Core.Domain`: 도메인 모델
* `Core.Application`: 비즈니스 로직, 유스케이스
* `Core.Infrastructure`: 외부 시스템 연동
* `Presentation.Views`: WPF View
* `Presentation.ViewModels`: WPF ViewModel
* `Presentation.Controls`: 커스텀 컨트롤

## 2. 명명 규칙

### 2.1 일반 원칙

| 요소           | 규칙              | 예시                      |
| -------------- | ----------------- | ------------------------- |
| 클래스         | PascalCase        | `TargetTrackingService` |
| 인터페이스     | I + PascalCase    | `ITargetRepository`     |
| 메서드         | PascalCase        | `UpdateTargetPosition`  |
| 프로퍼티       | PascalCase        | `CurrentPosition`       |
| 필드 (private) | _camelCase        | `_repository`           |
| 필드 (const)   | UPPER_SNAKE_CASE  | `MAX_TARGET_COUNT`      |
| 매개변수       | camelCase         | `targetId`              |
| 지역변수       | camelCase         | `isValid`               |
| 이벤트         | PascalCase + 동사 | `TargetPositionChanged` |

### 2.2 타입별 명명

#### 클래스

```csharp
// ✅ 좋은 예
public class TargetTrackingService { }
public class UserAuthenticationManager { }
public class SensorDataProcessor { }

// ❌ 나쁜 예
public class TargetTracking { } // 역할 불명확
public class Mgr { } // 축약 금지
public class Target_Tracking { } // 언더스코어 금지
```

#### 인터페이스

```csharp
// ✅ 좋은 예
public interface ITargetRepository { }
public interface ISecurityService { }
public interface IDisposable { }

// ❌ 나쁜 예
public interface TargetRepository { } // I 접두사 없음
public interface IITargetRepository { } // I 중복
```

#### 메서드

```csharp
// ✅ 좋은 예
public async Task<Target> GetTargetByIdAsync(string targetId)
public bool ValidatePosition(GeoPosition position)
public void OnTargetPositionChanged(object sender, EventArgs e)

// ❌ 나쁜 예
public async Task<Target> get_target(string id) // snake_case 금지
public bool Validate(GeoPosition p) // 매개변수명 불명확
public void targetPositionChanged() // camelCase 금지
```

### 2.3 Boolean 변수

```csharp
// ✅ 좋은 예
bool isValid;
bool hasPermission;
bool canExecute;
bool shouldRetry;
bool wasSuccessful;

// ❌ 나쁜 예
bool valid; // is/has/can 등 누락
bool permission; // 동사형 아님
bool execute; // 상태 불명확
```

### 2.4 컬렉션

```csharp
// ✅ 좋은 예
List<Target> targets; // 복수형
Dictionary<string, User> userById; // 의미 명확
IEnumerable<Sensor> activeSensors; // 형용사 + 복수형

// ❌ 나쁜 예
List<Target> targetList; // List 접미사 불필요
Dictionary<string, User> dict; // 의미 불명확
IEnumerable<Sensor> sensor; // 단수형
```

## 3. 코드 레이아웃

### 3.1 클래스 멤버 순서

```csharp
public class TargetTrackingService
{
    // 1. 상수
    private const int MAX_TARGET_COUNT = 500;
    private const int UPDATE_INTERVAL_MS = 100;
  
    // 2. 정적 필드
    private static readonly ILogger _staticLogger;
  
    // 3. 필드
    private readonly ITargetRepository _repository;
    private readonly ISecurityService _security;
  
    // 4. 생성자
    public TargetTrackingService(
        ITargetRepository repository,
        ISecurityService security)
    {
        _repository = repository;
        _security = security;
    }
  
    // 5. 프로퍼티
    public int CurrentTargetCount { get; private set; }
  
    // 6. 이벤트
    public event EventHandler<TargetEventArgs> TargetAdded;
  
    // 7. Public 메서드
    public async Task<Result> AddTargetAsync(Target target)
    {
        // 구현...
    }
  
    // 8. Protected 메서드
    protected virtual void OnTargetAdded(Target target)
    {
        TargetAdded?.Invoke(this, new TargetEventArgs(target));
    }
  
    // 9. Private 메서드
    private bool ValidateTarget(Target target)
    {
        // 구현...
    }
  
    // 10. 중첩 타입
    private class TargetComparer : IComparer<Target>
    {
        // 구현...
    }
}
```

### 3.2 들여쓰기 및 공백

```csharp
// 들여쓰기: 4칸 (탭 아님)
public class Example
{
    public void Method()
    {
        if (condition)
        {
            DoSomething();
        }
    }
}

// 연산자 앞뒤 공백
var result = a + b * c;
var isValid = (count > 0) && (count < MAX_COUNT);

// 쉼표 뒤 공백
Method(param1, param2, param3);

// 중괄호는 항상 새 줄
if (condition)
{
    // 코드...
}
else
{
    // 코드...
}
```

### 3.3 줄 길이

* **최대 120자**
* 초과 시 논리적 위치에서 줄바꿈

```csharp
// ✅ 좋은 예
var result = await _service
    .GetTargetDataAsync(targetId)
    .ConfigureAwait(false);

// 긴 메서드 시그니처
public async Task<ProcessingResult> ProcessTargetDataWithMultipleSensors(
    string targetId,
    IEnumerable<SensorData> sensorData,
    ProcessingOptions options,
    CancellationToken cancellationToken)
{
    // 구현...
}
```

## 4. XML 주석

### 4.1 필수 주석 요소

```csharp
/// <summary>
/// 표적의 위치를 업데이트합니다.
/// </summary>
/// <param name="targetId">표적 식별자 (TGT-XXX 형식)</param>
/// <param name="position">새로운 위치 정보 (WGS84 좌표계)</param>
/// <param name="cancellationToken">취소 토큰</param>
/// <returns>업데이트 결과</returns>
/// <exception cref="ArgumentNullException">
/// <paramref name="targetId"/> 또는 <paramref name="position"/>이 null인 경우
/// </exception>
/// <exception cref="SecurityException">
/// 현재 사용자가 표적 위치 업데이트 권한이 없는 경우
/// </exception>
/// <exception cref="TargetNotFoundException">
/// 지정한 ID의 표적을 찾을 수 없는 경우
/// </exception>
/// <remarks>
/// <para>보안 등급: 대외비</para>
/// <para>성능 요구사항: 50ms 이내 처리</para>
/// <para>관련 요구사항: REQ-TCS-001</para>
/// <para>
/// 이 메서드는 다음 작업을 수행합니다:
/// 1. 사용자 권한 검증
/// 2. 입력 데이터 유효성 검사
/// 3. 감사 로그 기록
/// 4. 데이터베이스 업데이트
/// 5. 이벤트 발행
/// </para>
/// </remarks>
/// <example>
/// 다음 예제는 표적 위치를 업데이트하는 방법을 보여줍니다:
/// <code>
/// var service = new TargetTrackingService(repository, security);
/// var position = new GeoPosition(37.5665, 126.9780);
/// var result = await service.UpdateTargetPositionAsync("TGT-001", position);
/// 
/// if (result.IsSuccess)
/// {
///     Console.WriteLine("업데이트 성공");
/// }
/// </code>
/// </example>
public async Task<UpdateResult> UpdateTargetPositionAsync(
    string targetId,
    GeoPosition position,
    CancellationToken cancellationToken = default)
{
    // 구현...
}
```

### 4.2 주석 작성 가이드

 **Summary** :

* 한 문장으로 목적 설명
* 마침표로 끝
* 명령형("~합니다")

 **Remarks** :

* 상세한 동작 설명
* 보안 등급, 성능 요구사항 명시
* 요구사항 추적 번호 포함

 **Example** :

* 실제 사용 가능한 코드
* 일반적인 사용 시나리오

## 5. 에러 처리

### 5.1 예외 사용 원칙

```csharp
// ✅ 좋은 예: 예외 상황에서만 예외 사용
public Target GetTargetById(string targetId)
{
    if (string.IsNullOrEmpty(targetId))
        throw new ArgumentNullException(nameof(targetId));
  
    var target = _repository.FindById(targetId);
    if (target == null)
        throw new TargetNotFoundException(targetId);
  
    return target;
}

// ✅ 좋은 예: 정상 흐름에는 Result 패턴 사용
public Result<Target> TryGetTargetById(string targetId)
{
    if (string.IsNullOrEmpty(targetId))
        return Result<Target>.Failure("Invalid target ID");
  
    var target = _repository.FindById(targetId);
    if (target == null)
        return Result<Target>.Failure("Target not found");
  
    return Result<Target>.Success(target);
}
```

### 5.2 예외 계층

```csharp
// 기본 예외
public class TacticalCommandException : Exception
{
    public TacticalCommandException(string message) : base(message) { }
    public TacticalCommandException(string message, Exception inner) 
        : base(message, inner) { }
}

// 도메인별 예외
public class TargetException : TacticalCommandException
{
    public TargetException(string message) : base(message) { }
}

public class TargetNotFoundException : TargetException
{
    public string TargetId { get; }
  
    public TargetNotFoundException(string targetId) 
        : base($"Target not found: {targetId}")
    {
        TargetId = targetId;
    }
}
```

### 5.3 예외 처리 패턴

```csharp
public async Task ProcessTargetAsync(string targetId)
{
    try
    {
        // 비즈니스 로직
        var target = await _repository.GetByIdAsync(targetId);
        await ProcessAsync(target);
    }
    catch (SecurityException ex)
    {
        // 보안 예외는 로그하고 재발생
        _logger.LogSecurityError(ex, "Unauthorized access attempt");
        throw;
    }
    catch (TargetNotFoundException ex)
    {
        // 비즈니스 예외는 처리
        _logger.LogWarning(ex, "Target not found: {TargetId}", targetId);
        return; // 또는 적절한 처리
    }
    catch (Exception ex)
    {
        // 예상치 못한 예외는 로그하고 재발생
        _logger.LogError(ex, "Unexpected error processing target");
        throw new TacticalCommandException("Processing failed", ex);
    }
}
```

## 6. 비동기 프로그래밍

### 6.1 Async/Await 사용

```csharp
// ✅ 좋은 예
public async Task<Target> GetTargetAsync(string targetId)
{
    // 비동기 I/O
    var data = await _repository.FetchAsync(targetId);
  
    // CPU 바운드 작업
    var processed = await Task.Run(() => ProcessData(data));
  
    // ConfigureAwait 사용 (라이브러리 코드)
    var validated = await ValidateAsync(processed)
        .ConfigureAwait(false);
  
    return validated;
}

// ❌ 나쁜 예
public Target GetTarget(string targetId)
{
    // async 없이 Wait() 사용 - 데드락 위험!
    return GetTargetAsync(targetId).Result;
}

public async Task<Target> GetTargetAsync(string targetId)
{
    // 동기 코드를 async로 감쌈 - 의미 없음
    return await Task.FromResult(_repository.Find(targetId));
}
```

### 6.2 취소 토큰

```csharp
// ✅ 모든 비동기 메서드에 CancellationToken 전달
public async Task<ProcessResult> ProcessDataAsync(
    byte[] data,
    CancellationToken cancellationToken = default)
{
    // 장시간 작업 전 취소 확인
    cancellationToken.ThrowIfCancellationRequested();
  
    // 비동기 작업에 토큰 전달
    var result = await _service
        .ProcessAsync(data, cancellationToken)
        .ConfigureAwait(false);
  
    return result;
}
```

## 7. LINQ 사용

### 7.1 메서드 구문 vs 쿼리 구문

```csharp
// ✅ 메서드 구문 선호 (간단한 경우)
var activeTargets = targets
    .Where(t => t.IsActive)
    .OrderBy(t => t.Priority)
    .Take(10);

// ✅ 쿼리 구문 (복잡한 조인)
var result = from target in targets
             join sensor in sensors on target.Id equals sensor.TargetId
             where target.IsActive && sensor.Status == SensorStatus.Online
             select new { Target = target, Sensor = sensor };
```

### 7.2 성능 고려사항

```csharp
// ❌ 나쁜 예: 중복 순회
var result = targets
    .Where(t => t.IsActive)
    .ToList() // 실체화 1
    .Where(t => t.Priority > 5)
    .ToList(); // 실체화 2

// ✅ 좋은 예: 단일 순회
var result = targets
    .Where(t => t.IsActive && t.Priority > 5)
    .ToList(); // 실체화 1

// ❌ 나쁜 예: Count() 후 Any()
if (targets.Count() > 0)
{
    // ...
}

// ✅ 좋은 예: Any() 사용
if (targets.Any())
{
    // ...
}
```

## 8. 금지 사항

### 8.1 절대 금지

```csharp
// ❌ 매직 넘버
if (count > 500) { }

// ✅ 상수 사용
private const int MAX_TARGET_COUNT = 500;
if (count > MAX_TARGET_COUNT) { }

// ❌ 하드코딩된 문자열
throw new Exception("Error occurred");

// ✅ 리소스 사용
throw new Exception(Resources.Errors.GeneralError);

// ❌ goto 문
goto label;

// ❌ 다중 return (단순한 경우 제외)
public int Calculate(int x)
{
    if (x < 0) return -1;
    if (x == 0) return 0;
    if (x > 100) return 100;
    return x;
}

// ✅ 단일 return
public int Calculate(int x)
{
    int result;
  
    if (x < 0)
        result = -1;
    else if (x == 0)
        result = 0;
    else if (x > 100)
        result = 100;
    else
        result = x;
  
    return result;
}
```

### 8.2 사용 자제

```csharp
// ⚠️ var: 타입이 명확할 때만
var target = new Target(); // ✅ OK
var count = GetCount(); // ❌ 타입 불명확

// ⚠️ 삼항 연산자: 단순할 때만
var status = isActive ? "Active" : "Inactive"; // ✅ OK
var result = a > b ? (c > d ? x : y) : (e > f ? z : w); // ❌ 복잡

// ⚠️ 람다 표현식: 짧을 때만
targets.Where(t => t.IsActive); // ✅ OK
targets.Where(t =>  // ❌ 너무 김 - 별도 메서드로
{
    // 10줄의 복잡한 로직
});
```

## 9. 체크리스트

커밋 전 확인:

* [ ] 파일 헤더 작성
* [ ] 네이밍 규칙 준수
* [ ] XML 주석 완비 (public 멤버)
* [ ] 요구사항 추적 번호 포함
* [ ] 예외 처리 적절
* [ ] 비동기 패턴 올바름
* [ ] 매직 넘버 없음
* [ ] 하드코딩된 문자열 없음
* [ ] 테스트 코드 작성
* [ ] 코드 리뷰 완료

```

계속해서 더 보여드릴까요? 다음으로 보여드릴 수 있는 내용:
- 실제 프로젝트 레이어별 CLAUDE.md
- .claudeignore 설정
- MCP 커스텀 서버 구현
- 통합 워크플로우 스크립트
- 실전 시나리오별 가이드

무엇을 더 보고 싶으신가요?
```
